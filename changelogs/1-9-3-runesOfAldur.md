# Runes of Aldur

## Bug fixes 🐛

- Fixes the side panel overlapping the trade site; the page now reserves space on the right so listings stay fully visible
- Fixes the panel appearing on both sides of the screen on the PoE 2 trade site
- Prevents duplicate side panels when the extension is reinjected during development

## Developer experience 🛠️

- Restores `npm install` and `npm run dev` on modern Node.js and npm versions
